// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    id 'com.android.application' version '7.3.1' apply false  // add to change the version to run for me
    id 'com.android.library' version '7.3.1' apply false   // add to change the version...
    id 'org.jetbrains.kotlin.android' version '1.8.0' apply false
}

